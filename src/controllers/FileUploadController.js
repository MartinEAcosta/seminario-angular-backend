const { response } = require('express');

const uploadFile = ( req , res = response ) => {
    console.log(req);
    try{

    }
    catch(error){

        
    }
}


const uploadMultipleFiles = ( req , res ) => {

    try{

    }
    catch(error){


    }
}

module.exports = {
    uploadFile,
    uploadMultipleFiles,
}